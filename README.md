# MentoPrime

This repository contains a simple Amazon Clone UI built with React via CDN scripts.
The React app is located in the `amazon-clone` directory.

## Preview

To preview the app locally, run:

```bash
cd amazon-clone
python3 -m http.server 3000
```

Then open `http://localhost:3000` in your browser.
